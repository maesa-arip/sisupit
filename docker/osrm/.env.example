# Base name berkas .osrm hasil praproses (osrm-extract/partition/customize),
# tersimpan di docker/osrm/data/ (mis. indonesia.osrm untuk data se-Indonesia).
OSRM_FILENAME=indonesia.osrm

# Port di host yang diteruskan ke OSRM (container selalu listen di 5000).
OSRM_HOST_PORT=5000
